[pytest]
pythonpath = src
markers =
    integration: marks tests as integration (deselect with '-m "not integration"')
    bootstrap: sanity checks for sys.path import viability and early-stage discovery
    compat: legacy alias and interface compatibility



